<Page x:Class="TurakasTest.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoginPage" Height="300" Width="300">
    <Grid Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0">
        <Rectangle  HorizontalAlignment="Left"  Stroke="Black" VerticalAlignment="Top" Fill="#FFE80C84" Focusable="True"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left"   TextWrapping="Wrap" Text="Insert youre very cool and unique username" VerticalAlignment="Top" Foreground="#FF210404" FontSize="16" FontWeight="Bold" />
        <TextBox x:Name="inputName"  Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" Text=" username"  Background="#FF55D419" IsEnabled="True" KeyDown="input_enter_pressed" />

    </Grid>
</Page>
